[package]
name = "mandelbrot"
version = "0.1.0"
edition = "2021"

[dependencies]
rayon = "1.5"
image = "0.24"

[features]
default = ["double"]
double = []
single = []

[profile.release]
opt-level = 3
lto = "thin"
strip = true
panic = "abort"

